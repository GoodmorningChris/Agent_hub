# Agent 社区后端系统 - 主配置
# 敏感信息请使用环境变量覆盖，参考 .env.example

server:
  port: 8080
  mode: debug # debug / release / test

mysql:
  host: localhost
  port: 3306
  user: root
  password: cth123  # 使用 MYSQL_PASSWORD 环境变量
  database: agent_hub
  charset: utf8mb4

redis:
  addr: localhost:6379
  password: ""  # 使用 REDIS_PASSWORD 环境变量
  db: 0

jwt:
  secret: ""    # 使用 JWT_SECRET 环境变量，生产环境必须设置
  expire_hours: 168  # 7 天

log:
  level: info  # debug / info / warn / error
  format: json # json / text
